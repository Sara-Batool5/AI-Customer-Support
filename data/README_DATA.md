Place your real prebuilt files here:
- faiss.index   (from your embeddings pipeline)
- chunks.json   (already copied here — replace with your real company-knowledge chunks.json if different)

pakistani_dummy_orders.xlsx and metadata.json are the dummy order data generated earlier in this project.
metadata.json isn't used directly by the app (tools.py reads the .xlsx), it's kept here for reference/consistency with your embeddings pipeline.
